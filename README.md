# React-Conditional-Rendering


```javascript
   <If condition={value === 5}>
       <Then>
           <Component/>
       </Then>
   </If>
```

```javascript
   <If condition={loading}>
       <Then>
           <Loader/>
       </Then>
       <Else>
           <Component>
       </Else>
   </If>
```
